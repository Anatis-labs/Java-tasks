package com.company;

import java.util.InputMismatchException;
import java.util.Scanner;

public class Main {


// 6.1 not completed
/*

static int getInteger(String text) {
        Scanner scanner = new Scanner(System.in);
        System.out.print(text);
        int number = 0;
        int range = 0;
        while (true) {

            try {
                number = scanner.nextInt();
                break;
            } catch (InputMismatchException ex) {
                System.out.println("please enter a number, not a fucking letter");
                scanner.next();
            }

        }
        return number;
    }

    static int getInteger(String text, int min, int max) {
        Scanner scanner = new Scanner(System.in);
        System.out.print(text);
        int number = 0;
        while (true) {

            try {
                number = scanner.nextInt();
            } catch (InputMismatchException ex) {
                System.out.println("please enter a number, not a fucking letter");
                scanner.next();
            }

            if (number <= max && number >= min)
                break;
            else {
                System.out.printf("enter a number between %s and %s", min, max);
            }
        }
        return number;
    }

    public static void main(String[] args) {

        int number = getInteger("Type any number you want: ");
        int range = getInteger("Type a number between 1 and 5: ", 1, 5);

        System.out.printf("Your number is %s.", number);
        System.out.printf("Your number is %s.", range);
    }
}
*/

// 6-3 completed
/*
    static String textfixer(String text) {
        
        int test = text.length();
        String t1 = text.substring(1, (test)).toLowerCase();
        String t2 = text.substring(0, 1).toUpperCase();
        return (t2 + t1);

    }

    public static void main(String[] args) {
        Scanner textscanner = new Scanner(System.in);
        System.out.println("Enter a name or a word, up to u");
        String text = textscanner.next();
        String fixedtext = textfixer(text);

        System.out.println(fixedtext);

    }
}
*/

/*
     static String reverser(String toBeReversed) {
        
        StringBuilder sb = new StringBuilder(toBeReversed);
        System.out.println("Reversed string is:");


        return sb.reverse().toString();
    }

    public static void main(String[] args) {
        System.out.println("Gonna reverse the text entered");

        String reversetext = reverser("1dds/ved/ tnuomnu");
        System.out.println(reversetext);
    }
}
*/
